version: '3.8'

services:
  db_test:
    image: postgres:15
    container_name: mini-wallet-db-test
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: walletdb_test
    ports:
      - '5433:5432'
    tmpfs:
      - /var/lib/postgresql/data